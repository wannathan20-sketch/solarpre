[variables]
PYTHONUNBUFFERED = "1"

[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r solar/requirements.txt"]

[start]
cmd = "cd solar && uvicorn app:app --host 0.0.0.0 --port ${PORT}"
